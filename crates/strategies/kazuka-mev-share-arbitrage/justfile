set dotenv-load

# deploy:
#   TODO

bindings:
  forge bind \
    --bindings-path ./bindings \
    --root ./contracts \
    --crate-name kazuka-mev-share-arbitrage-bindings \
    --crate-description "MEV-share arbitrage bindings for kazuka" \
    --alloy-version "1.0" \
    --overwrite
